{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "serve:user-api": "npm start",
    "start": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "json-server": "^0.15.1"
  }
}
